describe("Pitch Palette Beginner", () => {

  test("Test pitch sol", () => {
    const pitch = "sol";
    expect(pitch).toBe("sol");
  });

  test("Test pitch la", () => {
    const pitch = "la";
    expect(pitch).toBe("la");
  });

  test("Test pitch do", () => {
    const pitch = "do";
    expect(pitch).toBe("do");
  });

});
